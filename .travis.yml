sudo: false
language: node_js
node_js:
  - '6'
env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
before_install:
- npm install -g yarn gulp bower typescript
install:
- yarn install
- bower install -F
- gulp build
